#include "main.h"

/**
 * print_rev-Prints string in reverse
 * @s:input
 */
void print_rev(char *s)
{
	int count, num;

	while (*s)
		num++;
	for (count = num; count >= 0; count--)
	{
		_putchar(s[count]);
	}
	_putchar(10);
}
